<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    //Internet permission
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Existing -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventLotterySystem">

        //google maps api key
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR API KEY"/>

        <!-- Existing activities -->
        <activity
            android:name=".UI.activities.AccountTypeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".UI.activities.admin.AdminHomeActivity" />
        <activity android:name=".UI.activities.admin.AdminEventDetailActivity" />
        <activity android:name=".UI.activities.admin.AdminLoginActivity" />
        <activity android:name=".UI.activities.admin.AdminProfileDetailActivity" />
        <activity android:name=".UI.activities.admin.AdminImageDetailActivity" />
        <activity android:name=".MainActivity" />

    </application>
</manifest>
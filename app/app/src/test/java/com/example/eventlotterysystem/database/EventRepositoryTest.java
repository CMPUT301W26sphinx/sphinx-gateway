package com.example.eventlotterysystem.database;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;

public class EventRepositoryTest {

    /**
    @Test
    public void getEventsReturnsListOfEvents() {
        EventRepository repository = new EventRepository();
        List<Event> events = repository.getEvents();

        assertFalse(events.isEmpty());
    }

    @Test
    public void getEventsReturnsThreeSampleEvents() {
        EventRepository repository = new EventRepository();
        List<Event> events = repository.getEvents();

        assertEquals(3, events.size());
    }

    @Test
    public void firstEventTitleIsSwimmingLessons() {
        EventRepository repository = new EventRepository();
        List<Event> events = repository.getEvents();

        assertEquals("Swimming Lessons", events.get(0).getTitle());
    }
    */
}